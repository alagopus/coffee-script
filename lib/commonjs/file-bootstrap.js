// Generated by CoffeeScript 1.5.0-pre
(function() {
  var altSeparatorCached, exports, separatorCached, separatorReCached, system,
    __slice = [].slice;

  exports = require("file");

  system = require("system");

  if (/\bwindows\b/i.test(system.os) || /\bwinnt\b/i.test(system.os)) {
    exports.ROOT = "\\";
    exports.SEPARATOR = "\\";
    exports.ALT_SEPARATOR = "/";
  } else {
    exports.ROOT = "/";
    exports.SEPARATOR = "/";
    exports.ALT_SEPARATOR = undefined;
  }

  exports.SEPARATORS_RE = (function(){ function _SEPARATORS_RE() {
    var altSeparatorCached, separatorCached, separatorReCached, _SEPARATORS_RE;
    if (separatorCached !== exports.SEPARATOR || altSeparatorCached !== exports.ALT_SEPARATOR) {
      separatorCached = exports.SEPARATOR;
      altSeparatorCached = exports.ALT_SEPARATOR;
      separatorReCached = new RegExp("[" + (separatorCached || "").replace(/[-[\]{}()*+?.\\^$|,#\s]/g, "\\$&") + (altSeparatorCached || "").replace(/[-[\]{}()*+?.\\^$|,#\s]/g, "\\$&") + "]", "g");
    }
    return separatorReCached;
  }; return _SEPARATORS_RE})();

  separatorCached = void 0;

  altSeparatorCached = void 0;

  separatorReCached = void 0;

  exports.join = (function(){ function _join() {
    var args, _join;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    if (args.length === 1 && args[0] === "") {
      return exports.SEPARATOR;
    }
    return exports.normal(Array.prototype.join.call(args, exports.SEPARATOR));
  }; return _join})();

  exports.split = (function(){ function _split(path) {
    var parts, _split;
    parts = void 0;
    try {
      parts = String(path).split(exports.SEPARATORS_RE());
    } catch (exception) {
      throw new Error("Cannot split " + (typeof path) + ", \"" + path + "\"");
    }
    if (parts.length === 1 && parts[0] === "") {
      return [];
    }
    return parts;
  }; return _split})();

  exports.resolve = (function(){ function _resolve() {
    var args, children, i, j, leaf, parents, part, parts, path, root, _resolve;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    root = "";
    parents = [];
    children = [];
    leaf = "";
    i = 0;
    while (i < args.length) {
      path = String(args[i++]);
      if (path === "") {
        continue;
      }
      parts = path.split(exports.SEPARATORS_RE());
      if (exports.isAbsolute(path)) {
        root = parts.shift() + exports.SEPARATOR;
        parents = [];
        children = [];
      }
      leaf = parts.pop();
      if (leaf === "." || leaf === "..") {
        parts.push(leaf);
        leaf = "";
      }
      j = 0;
      while (j < parts.length) {
        part = parts[j];
        if (part === "." || part === "") {

        } else if (part === "..") {
          if (children.length) {
            children.pop();
          } else {
            if (!root) {
              parents.push("..");
            }
          }
        } else {
          children.push(part);
        }
        j++;
      }
    }
    path = parents.concat(children).join(exports.SEPARATOR);
    if (path) {
      leaf = exports.SEPARATOR + leaf;
    }
    return root + path + leaf;
  }; return _resolve})();

  exports.normal = (function(){ function _normal(path) {
    var _normal;
    return exports.resolve(path);
  }; return _normal})();

  exports.isAbsolute = (function(){ function _isAbsolute(path) {
    var parts, _isAbsolute;
    parts = exports.split(path);
    if (parts.length === 0) {
      return false;
    }
    return exports.isDrive(parts[0]);
  }; return _isAbsolute})();

  exports.isRelative = (function(){ function _isRelative(path) {
    var _isRelative;
    return !exports.isAbsolute(path);
  }; return _isRelative})();

  exports.isDrive = (function(){ function _isDrive(first) {
    var _isDrive;
    if (/\bwindows\b/i.test(system.os) || /\bwinnt\b/i.test(system.os)) {
      return /:$/.test(first);
    } else {
      return first === "";
    }
  }; return _isDrive})();

  /*
  root
  returns the Unix root path
  or corresponding Windows drive
  for a given path.
  */


  exports.root = (function(){ function _root(path) {
    var parts, _root;
    if (!exports.isAbsolute(path)) {
      path = require("file").absolute(path);
    }
    parts = exports.split(path);
    return exports.join(parts[0], "");
  }; return _root})();

  exports.dirname = (function(){ function _dirname(path) {
    var parts, _dirname;
    parts = exports.split(path);
    parts.pop();
    return exports.join.apply(null, parts) || ".";
  }; return _dirname})();

  exports.basename = (function(){ function _basename(path, extension) {
    var chop, _basename;
    chop = (function(){ function _chop(a, b) {
      var start, _chop;
      if (!b) {
        return a;
      }
      start = a.length - b.length;
      if (a.lastIndexOf(b, start) === start) {
        return a.substring(0, start);
      } else {
        return a;
      }
    }; return _chop})();
    return chop(/[^\/\\]+$/.exec(path)[0], extension);
  }; return _basename})();

  exports.extension = (function(){ function _extension(path) {
    var index, _extension;
    path = exports.basename(path);
    path = path.replace(/^\.*/, "");
    index = path.lastIndexOf(".");
    if (index <= 0) {
      return "";
    } else {
      return path.substring(index);
    }
  }; return _extension})();

}).call(this);
